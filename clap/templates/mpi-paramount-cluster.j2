clusters:
  mpi-paramount-cluster:
    nodes:
      mpi-coordinator:
        type: {{ node_type_coord }}
        count: 1

      {% if node_count and node_count > 0 %}
      mpi-slave:
        type: {{ node_type }}
        count: {{ node_count }}

      {% endif %}
