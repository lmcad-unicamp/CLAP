---
- gather_facts: no
  hosts: localhost
  tasks: 
  - name: {{ task_name }}
    ec2: 
      instance_ids:
      {% for instance_id in instance_ids %}
      - {{ instance_id }}
      {% endfor %}
      state: {{ state }}
      wait: {{ wait }}
